spring:
    datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/sell?characterEncoding=utf-8
      username: root
      password: root
    jpa:
      show-sql: true
server:
  servlet:
    context-path: /sell